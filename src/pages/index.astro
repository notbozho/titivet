---
import BaseLayout from "@/layouts/BaseLayout.astro";
import ImageMod from "@/layouts/components/ImageMod.astro";
import AboutMe from "@/layouts/sections/AboutMe.astro";
import Cabinet from "@/layouts/sections/Cabinet.astro";
import Services from "@/layouts/sections/Services.astro";
---

<BaseLayout>
  <section class="section relative overflow-hidden">
    <div class="mt-18"></div>
    <div
      class="from-primary/20 absolute inset-0 -z-10 bg-gradient-to-br via-transparent to-transparent"
    >
    </div>

    <!-- Decorative -->
    <div
      class="animate-float-slow absolute top-32 left-[-40px] opacity-10 blur-[2px]"
    >
      <ImageMod src="/images/paw.svg" alt="" width={80} height={80} />
    </div>

    <div
      class="animate-float-slower absolute top-1/3 right-4 rotate-6 opacity-5"
    >
      <ImageMod src="/images/paw.svg" alt="" width={70} height={70} />
    </div>

    <div
      class="animate-float-slower absolute bottom-12 left-[80px] rotate-12 opacity-10"
    >
      <ImageMod src="/images/paw.svg" alt="" width={100} height={100} />
    </div>

    <div
      class="container mx-auto grid grid-cols-1 items-center gap-8 text-center md:grid-cols-2 md:text-start"
    >
      <div class="order-2 flex flex-col gap-y-8 md:order-1">
        <h1 class="text-4xl leading-tight font-bold md:text-5xl">
          Ветеринарен Кабинет <br />
          <span class="text-primary">д-р Станислава Андреева</span>
        </h1>
        <p class="text-lg">
          Професионална грижа и лично отношение към всеки домашен любимец. От
          2014 г. помагаме на животните да живеят по-здрави и щастливи животи
          чрез модерна диагностика, сигурно лечение и истинска отдаденост.
        </p>
      </div>
      <div class="order-1 h-full w-full md:order-2">
        <ImageMod
          src="/images/hero.png"
          alt="Hero Image"
          width={600}
          height={900}
          widths={[360, 600, 900]}
          sizes="(max-width: 768px) 360px, 600px"
          loading="eager"
          decoding="async"
          format="avif"
          fetchpriority="high"
          class="h-full w-full rounded-lg object-cover"
        />
      </div>
    </div>
  </section>
  <Services />
  <AboutMe />
  <Cabinet />
</BaseLayout>
